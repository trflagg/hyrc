type Query {
  hello: String
  messageList: [Message]!
}

type Message {
  id: String!
  name: String!
  text: String
}

input MessageInput {
  id: String!
  name: String!
  text: String!
}

type Mutation {
  createOrUpdateMessage(message: MessageInput!): Message!
  deleteMessage(message: MessageInput!): Message!
}
